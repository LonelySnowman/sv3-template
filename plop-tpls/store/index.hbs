import { defineStore } from 'pinia';
import pinia from '@/store';
import { {{ properCase storeName }}State } from './types';

export const use{{ properCase storeName }}StoreHook = defineStore(
'{{ properCase storeName }}',
  {
    state: () => ({
      info: ''
    }),
    getters: {},
    actions: {
    updateInfo(partial: Partial<{{ properCase storeName }}State>) {
      this.$patch(partial);
      },
    },
    persist: {
      key: 'info',
      storage: sessionStorage,
      paths: ['info'],
    },
  }
);


export function use{{ properCase storeName }}Store() {
  return use{{ properCase storeName }}StoreHook(pinia);
}